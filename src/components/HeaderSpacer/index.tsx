import React from "react"
import styled from "styled-components";
import sizes from "@/constants/sizes";

const HeaderSpacer = styled.div`
  height: ${sizes.navPxHeight}px;
`

export default HeaderSpacer
